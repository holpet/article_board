import { describe, expect, it } from "vitest";

describe("capitalizeText", () => {
  // TODO: how? - "document" is undefined
  it("should capitalize text", () => {
    expect(true).toBe(true);
  });
});
